#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main(int argc, char const *argv[]) {
  int count = 0;
  char c;
  while ((c = getchar()) != EOF) {
    if(isAlpha(c)){
      
    }
  }
  return 0;
}
